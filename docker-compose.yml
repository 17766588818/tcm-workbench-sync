services:
  tcm-sync:
    build: .
    container_name: tcm-sync
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - ACCESS_KEY=请改成你自己的强密钥
      - DATA_FILE=/app/data/db.json
    volumes:
      - ./data:/app/data

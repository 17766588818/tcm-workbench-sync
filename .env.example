# 复制为 .env 并修改（docker-compose / 本地运行可用）
PORT=3000
# 重要：请改成你自己的强密钥，App 端需填写一致的值
ACCESS_KEY=请改成你自己的强密钥
# 数据文件位置（容器内请用 /app/data/db.json 并挂载卷）
DATA_FILE=./data/db.json
# 可选：静态前端目录（放入前端后，一个 URL 同时提供 App + API）
PUBLIC_DIR=./public
